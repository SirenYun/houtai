<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.OrderitemMapper">
    
    <insert id="insertOrderitem">
    	insert into orderitems(orderId,productId,productName,size,count,price) values (#{orderId},#{productId},#{productName},#{size},#{count},#{price})
    </insert>
    
    <select id="selectOrderitemById" resultType="model.Order">
    	select * from orderitem where orderId = #{orderId}
    </select>
    
    <select id="deleteOrderitem" resultType="model.Order">
    	delete from orderitem where orderitemId = #{orderitemId}
    </select>
</mapper>